import React from 'react';
import logo from '../assets/sentinel_icon.png';

const tokenTypes = [{
    value: 'TSENT',
    name: 'TSENT',
    img: <img alt="Sent log" src={logo}/>,
}];

export default tokenTypes;
