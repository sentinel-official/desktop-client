import React from 'react';
import './index.css';

const Socials = (props) => {
    return (
        <div className={'socials'}>
            *socials*
        </div>
    );
};

export default Socials;
