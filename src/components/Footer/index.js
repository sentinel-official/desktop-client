import React from 'react';
import Socials from './Socials';
import './index.css';

const Footer = (props) => {
    return (
        <div className={'footer'}>
            <Socials/>
        </div>
    );
};

export default Footer;
