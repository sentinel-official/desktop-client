import React from 'react';
import Checkbox from '../../components/Checkbox';

const Note = () => {
    return (
        <Checkbox
            className="custom-control-input"
        />
    );
};

export default Note;
